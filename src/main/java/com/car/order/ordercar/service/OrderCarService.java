package com.car.order.ordercar.service;

public class OrderCarService {

    /// zapisywanie do bazy private Repository repository;

    public boolean orderCar(String username) {
        // wywolanie
        String reservation = "nowa_rezerwacja";
//        checkBalance -> boolean result = repository.checkUsersdfkndsg(username);
//        if (result == true) {
//        rezerwuj
//        } else { throw asfsa
//    }
//        saveToDb()

        return true;
    }
}
